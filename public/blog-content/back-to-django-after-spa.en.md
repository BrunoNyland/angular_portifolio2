# Why I came back to Django after 2 years of pure SPA

On the false promise of JS on the server and when the monolith with templates still wins.

<!-- TODO: write the full body of this post in Markdown. Supports headings, lists,
     links, quotes and fenced code blocks with syntax highlighting (```ts, ```python…). -->
