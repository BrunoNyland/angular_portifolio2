# Pandas is a language, not a library

I learned more about programming solving CSVs in Pandas than in any algorithms book.

<!-- TODO: write the full body of this post in Markdown. Supports headings, lists,
     links, quotes and fenced code blocks with syntax highlighting (```python…). -->
